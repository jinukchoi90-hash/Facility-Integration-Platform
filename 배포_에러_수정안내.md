# GitHub 재배포 전 꼭 읽어주세요

## 이번에 바꾼 것: packages.txt를 완전히 뺐습니다

계속 나던 이 에러:
```
E: Release file for .../bullseye-security/InRelease is expired
❗️ installer returned a non-zero exit code
```

이건 코드 문제가 아니라 **Streamlit Cloud 서버가 `packages.txt`에 적힌
시스템 프로그램(libreoffice, tesseract)을 설치하려고 `apt-get`을 돌리는데,
그 저장소 목록이 계속 만료된 상태라 생기는 인프라 쪽 문제**였어요.
재부팅해도 또 만료되는 걸 몇 번 겪으셨을 거예요.

**확인해보니 이 두 프로그램이 없어도 앱은 안 죽습니다.** 코드에 이미
안전장치가 있어서, 관련 기능만 못 쓰고 나머지는 전부 정상 작동해요.
그래서 아예 `packages.txt`를 빼서, 이 에러 자체가 날 원인을 없앴습니다.

## 이번 배포로 못 쓰게 되는 기능 (딱 2개)

1. **보고서 화면의 "PDF로도 받기" 버튼** — Word(.docx) 다운로드는 그대로 되고,
   PDF 변환만 "서버에 LibreOffice가 필요합니다" 안내로 바뀝니다.
2. **사진에서 숫자 자동으로 읽기(OCR)** — 축정렬/진동측정 화면에서
   측정기 사진 찍어 올리면 자동으로 숫자를 읽어주던 기능. 이제 그 사진은
   못 올리고, 값을 손으로 직접 입력하시면 됩니다.

**나머지 전부(설비관리, CBM, 오버홀, 경상정비, 공정일보, 각종 보고서 생성 등)는
그대로 다 됩니다.**

## GitHub에 반영하는 법

1. 이 zip 안의 `pump_grade_program.py`로 기존 파일을 덮어쓰기
2. 저장소에서 **`packages.txt` 파일 자체를 삭제**하고 커밋·push
   (파일이 아예 없어야 합니다. 빈 파일로 두면 소용없어요)
3. Streamlit Cloud가 자동으로 다시 배포합니다

## 나중에 OCR·PDF변환을 다시 살리고 싶으시면

지금 당장은 배포부터 되게 하는 게 우선이니 뺐고, 나중에 Streamlit Cloud의
데비안 이미지 문제가 풀리면(보통 몇 주~몇 달 걸릴 수도 있고, Streamlit이
언제 이미지를 갱신할지는 저희도 알 수 없어요) `packages.txt`를 다시
```
libreoffice-writer
tesseract-ocr
tesseract-ocr-kor
```
이렇게 만들어서 추가하시면 원래대로 돌아옵니다.
